apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "manifest.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "manifest.name" . }}
    helm.sh/chart: {{ include "manifest.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "manifest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
